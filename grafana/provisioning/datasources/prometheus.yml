apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy # Grafana's own backend calls Prometheus; the browser never talks to it directly
    url: http://prometheus:9090 # Docker service name, same reasoning as ingestion:2112
    isDefault: true
